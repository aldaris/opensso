<?xml version="1.0" encoding="UTF-8"?>

<!--
   DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS HEADER.
  
   Copyright (c) 2007 Sun Microsystems Inc. All Rights Reserved
  
   The contents of this file are subject to the terms
   of the Common Development and Distribution License
   (the License). You may not use this file except in
   compliance with the License.

   You can obtain a copy of the License at
   https://opensso.dev.java.net/public/CDDLv1.0.html or
   opensso/legal/CDDLv1.0.txt
   See the License for the specific language governing
   permission and limitations under the License.

   When distributing Covered Code, include this CDDL
   Header Notice in each file and include the License file
   at opensso/legal/CDDLv1.0.txt.
   If applicable, add the following below the CDDL Header,
   with the fields enclosed by brackets [] replaced by
   your own identifying information:
   "Portions Copyrighted [year] [name of copyright owner]"

   $Id: build-tools.xml,v 1.12 2008-06-25 05:48:44 qcheng Exp $

-->

<project name="openfmtools" default="all" basedir=".">
    <description>Open Federation: Tools.zip Creation</description>

    <!-- enforce invocation via top level build only -->
    <fail message="*** tools may be built from top level only ***">
        <condition>
            <not><isset property="openfm.built.dir"/></not>
        </condition>
    </fail>

    <property name="tools.temp.dir" value="${openfm.built.dir}/.tools" />

    <target name="all">
        <delete dir="${tools.temp.dir}" />
        <mkdir dir="${tools.temp.dir}" />
        <unzip src="${openfm.extlib.dir}/amAdminTools.zip"
            dest="${tools.temp.dir}" />

        <copy file="${openfm.dir}/license.txt"
            tofile="${tools.temp.dir}/license.txt" />
        <delete file="${tools.temp.dir}/README.amadm" />
        <delete file="${tools.temp.dir}/template/unix/bin/amadm.template" />
        <delete file="${tools.temp.dir}/template/windows/bin/amadm.bat.template" />
        <replace file="${tools.temp.dir}/README.setup" token="amAdminTools"
            value="famAdminTools" />

        <copy todir="${tools.temp.dir}/lib">
            <fileset file="${fm.dist.dir}/fam.jar" />
            <fileset file="${fedlibrary.dist.dir}/openfedlib.jar" />
            <fileset dir="${extlib.dir}" includes="FastInfoset.jar, webservices-tools.jar, jsr173_api.jar, relaxngDatatype.jar, xalan.jar, xmlsec.jar, xsdlib.jar" />
        </copy>
        <copy todir="${tools.temp.dir}/locale" flatten="true">
            <fileset dir="${openfm.res.i18n.dir}" includes="**/*.properties"/>
            <fileset dir="${library.res.i18n.dir}" includes="**/*.properties"/>
        </copy>
        <copy file="${openfm.dir}/scripts/bin/famadm"
            tofile="${tools.temp.dir}/template/unix/bin/famadm.template" />
        <copy file="${openfm.dir}/scripts/bin/famadm.bat"
            tofile="${tools.temp.dir}/template/windows/bin/famadm.bat.template" />
        <zip destfile="${fm.dist.dir}/famAdminTools.zip" whenempty="create">
            <zipfileset dir="${tools.temp.dir}" filemode="755">
                <include name="setup"/>
            </zipfileset>
            <zipfileset dir="${tools.temp.dir}">
                <exclude name="setup"/>
            </zipfileset>
        </zip>

        <checksum file="${fm.dist.dir}/famAdminTools.zip"
            algorithm="SHA" fileext=".sha" />
        <delete dir="${tools.temp.dir}" />
    </target>

    <target name="clean">
        <delete dir="${tools.temp.dir}" />
    </target>
</project>
